rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // With all data access migrated to TanStack Server Functions using the 
    // Firebase Admin SDK, direct client access to Firestore is completely disabled.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
