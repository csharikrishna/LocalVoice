/**
 * Feature Flags and System Configurations
 * Use this file to toggle features globally without deleting code.
 */

// Note: SLA Tracking is now controlled dynamically via UI toggle (saved in localStorage).

// SLA Thresholds (in hours)
export const SLA_WARNING_HOURS = 24;
export const SLA_BREACH_HOURS = 48;
